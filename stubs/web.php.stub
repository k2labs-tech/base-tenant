<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Application Routes
|--------------------------------------------------------------------------
|
| Authentication and main routes are handled by the Base Tenant package.
| Add your custom application routes below.
|
*/

// Redirect root to dashboard
Route::get('/', function () {
    return redirect()->route('base-tenant.dashboard');
})->middleware('auth');

// Public home for guests
Route::get('/', function () {
    return redirect()->route('base-tenant.welcome');
})->middleware('guest');
